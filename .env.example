# 通用大模型（OpenAI 兼容接口）配置
# 复制本文件为 .env 并填入真实 Key 即可启用真实意图识别。
# 不填 VITE_LLM_API_KEY 时，程序自动回退到本地关键词 mock（默认置信度 0.95，保持原有自动落卡行为）。

# 默认接入 DeepSeek。把这里换成你的 DeepSeek API Key：
# https://platform.deepseek.com/api_keys
# ⚠️ 要么填真实 key，要么保持完全为空 —— 不要留 sk-... 这类占位符。
#    程序只判断「非空」就认为配了 key，占位符会导致请求 401 后报错回退，
#    反而不如留空（留空是干净地走本地关键词 mock）。
VITE_LLM_API_KEY=

# 可选：自定义接口地址（不填默认 https://api.deepseek.com）。
# 切换其他兼容厂商示例：
#   DeepSeek:    https://api.deepseek.com
#   腾讯混元:    https://api.hunyuan.cloud.tencent.com/v1
#   通义千问:    https://dashscope.aliyuncs.com/compatible-mode/v1
VITE_LLM_BASE_URL=

# 可选：模型名（默认 deepseek-chat）
VITE_LLM_MODEL=

# ─────────────────────────────────────────────
# ⚠️ 改完本文件必须【完整重启】开发服务（npm run electron:dev）。
#    .env 和 vite.config.ts 都只在 Vite 启动时读取一次，热更新不会生效；
#    若 5173 端口还被旧 Vite 进程占用，你看到的仍是旧配置。
# 说明：开发模式下请求走 Vite 代理 /api/llm（避开 CORS），
#      代理目标由 vite.config.ts 读取 VITE_LLM_BASE_URL 决定，
#      所以换厂商只改这里的 VITE_LLM_BASE_URL 即可，无需改代码。
